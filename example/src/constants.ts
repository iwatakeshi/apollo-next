/**
 * The property name used to store Apollo Client state in Next.js page props.
 * This is automatically injected by `withApollo` and consumed by `useApollo`.
 * 
 * @constant
 * @type {string}
 */
export const APOLLO_STATE_PROP_NAME = "__APOLLO_STATE__";
